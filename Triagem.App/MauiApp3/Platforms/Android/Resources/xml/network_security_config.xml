<?xml version="1.0" encoding="utf-8"?>
<!--
  Permite tráfego HTTP puro (sem TLS) só para os hosts usados em desenvolvimento
  local (emulador e dispositivo físico na mesma rede). O Android bloqueia HTTP puro
  por padrão a partir da API 28 — sem isso, ApiService.BaseUrl (http://10.0.2.2:5036
  em DEBUG) é rejeitado pelo próprio SO antes de sair do app.
  Em builds de Release, ApiService.UrlProducao deve ser HTTPS (o Android bloqueia
  cleartext para qualquer domínio fora da lista abaixo, então a URL de produção real
  precisa de certificado válido de qualquer forma).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- IP do PC na rede local (Wi-Fi) — usado quando o app roda num celular
             físico DEBUG apontando para a API rodando no PC. Trocar se o IP mudar. -->
        <domain includeSubdomains="false">192.168.1.19</domain>
    </domain-config>
</network-security-config>
