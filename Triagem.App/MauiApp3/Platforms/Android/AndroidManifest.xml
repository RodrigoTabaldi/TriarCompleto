<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<!-- allowBackup=false: dados de saúde (nome de paciente, respostas) não devem sair
	     do aparelho via backup automático do Google/adb backup — vale tanto para o app
	     normal (cache local só efêmero) quanto, principalmente, para o modo de
	     demonstração offline, onde o SQLite local (BancoLocal.cs) grava esses dados sem
	     a criptografia de campo que a API aplica. -->
	<application android:allowBackup="false" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true" android:networkSecurityConfig="@xml/network_security_config"></application>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
</manifest>